# 正式对照中止：电源边界校验失败

本批固定24面板正式实验于北京时间2026年9月13日01:39:29结束，状态为`failed`。
第15个面板（编号`p14`）的计时开启、2 worker、无spill测量，在执行前记录`AC`，
执行后记录`Battery`，触发原执行源码`acceptance.py:45`的电源保护。
具体记录：[失败测量](p14/on/w2-s0-formal-0/measurement.json)。

已通过逐次校验并保留在summary中的记录为456次正式测量、232次预热，共688条。
原始measurement文件共689份；689条SQL均返回成功，但最后一条在电源校验中被拒绝，
没有加入summary。SQL执行成功与样本符合性能协议是不同的状态。
完整结束14个面板，第15个面板部分执行；计划要求的24个面板没有完成。

离线核对确认，保留记录与原始文件一致，原始文件恰为预定排程的前689个事件，
前688条记录的测量前后电源均为AC。全部15个已创建面板的60个实例均有独立身份，
60份cleanup记录均为`complete`、`errors=[]`。本离线审计未查询实时Docker状态。
完整核对和原文件摘要见[failure-audit.json](failure-audit.json)。

这是环境失效导致的不完整实验，不是性能通过，也不是诊断性能回归的结论。
本次审计没有计算任何组效应或比值，没有进行正式统计分析，没有修改原始记录、
协议、原源码归档或原汇总。若恢复接电后另行执行，应建立独立且完整的24面板运行；
不得从本批续接、拼接、挑选或合并样本。
