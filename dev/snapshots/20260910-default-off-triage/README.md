# 默认关闭性能信号定位：独立快照

本轮保留9/6 C候选和9/10前两轮的所有脚本、原始结果与审计文件，新增的只是
默认关闭成本复现实验、测试和工作留痕。不推送，不改上游，不连接业务库。

- `freeze.py` / `start-manifest.json`：正式前验证上一轮1121项文件指纹、C候选
  和83项工具测试；记录新源码与协议留痕的哈希。不能重复执行来覆盖起点。
- `tooling-at-start.tar.gz`：正式前源码与初始诊断方案；后续文档更新另行记账。
- `audit.py`：从逐次日志与测量独立验证协议、汇总、身份和清理；默认只检查，
  `--final` 仅用于最终写入一次新审计清单。它不是人工Review或C回归重跑。
- 正式结果：`experiments/hnsw_build/results/20260910T144549Z-bounded-triage/`。
- smoke：`20260910T084218Z-bounded-triage-smoke/`；仅流程验证，不与正式统计合并。
  smoke后仅加强新入口的汇总校验和报告限制、增加5项测试；正式输入另行归档。

正式协议固定180次构建（108正式、72预热），36个临时环境；A的统计单位为
每场景6个配对块，不是18个独立位置。B是6种排列的描述性分析。
`status=passed` 仅表示协议完成或自动审计通过，不表示开销达标或项目已交付。

9/11收尾：正式108+72次、smoke18+12次全部完成并分别复核，36+6个临时环境
清理；正式12次观察均采到进度。完整环境83/83，系统Python77通过、6跳过。
`audit.json` 记录原始日志/内部时间边界/统计/排列复算、代码身份、旧文档更正
清单与最终文件指纹；没有重写旧审计，C仍为9/6候选，没有重新运行历史C测试。

报告入口：[方案、结果与边界](../../../docs/2026-09-10-default-off-triage.md)。
