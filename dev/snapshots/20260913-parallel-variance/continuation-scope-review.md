# 本轮继续范围的独立只读审查

审查时间：2026-09-13T02:56:26.891809+00:00。范围为 README、开发收尾、整体 Review、既有正式/机制证据，以及 HNSW 阶段热路径、正式统计报告和 CPU 校准工具的定点复核。没有运行负载或测试，没有修改产品源码、测量原件、冻结协议。它不是全仓库无缺陷证明。

## 结论与已完成范围

- **没有发现尚未修复、必须阻止当前功能候选交付准备的确定性产品 C 缺陷。**三项并行条件未建立低于 5% 的支持，属于性能证据未关闭，不能直接推出三个待修 bug。C/TAP 五文件与 9/11 构建 manifest 全部相符，沿用同一 9/6 候选的既有 Linux 验证，不声称本轮重跑。
- 约定阶段进度、默认关闭的内部计时、spill 上下文、报告/召回工具和已有任务只读观察已经实现并有范围对应的验证。不存在用户必须先提供业务库才能完成观察功能的缺项。[收尾记录](../../../docs/2026-09-12-development-closeout.md#剩余工作与当前停止边界)第 200–209 行明确区分这两者。
- 完整正式结果仍为 9/12 项 supported；并行无 spill 关闭/开启计时上界分别为 **7.5957% / 7.8604%**，并行 spill 开启计时 **5.5381%**，均为 not_established。没有任何项 regression_supported；这也不是三项已通过。正式 summary SHA256 仍为 `b7f221d7b8bbfa872f3b842f2ff267809bc4f1fd896813fac5bc94a2b3ddb1d3`。

## 确定性问题与假说应分开

1. **浮点 5% 边界已有修复。**当前 `overhead_crossover.py:75–84` 对上下界都保守排除与 1.05 近等的浮点数；独立审计和已有边界测试证据已记录。无需再以旧缺陷为待办，也不能改旧测量。
2. **Docker create 超时后可能遗留自有实例的问题已有修复。**`run.py:79–99, 253–255, 297–318` 在 create 前留存 name/nonce/image，以完整归属证据恢复 ID 后清理；未知归属/daemon 错误保留失败。既有 3 项离线 mock 覆盖真实 finally 路径，见 [修复证据](../../cpu_calibration/review-timeout-cleanup/report.md)。本审查没有重新执行测试。
3. **共享阶段终态读取是确定冗余，收益未证明。**`hnswbuild.c:653–654` 在 leader 已发布 LOAD_DISK 后仍调用读取；省略读取只覆盖 leader 剩余磁盘 tuple，典型约一万次，不覆盖两项无 spill 验收。当前 [草案与语义证明](c-hot-path-review.md)未应用，保持如此合理。phase 与常写锁可能共 cache line 是可检验机制，布局变化本身不是 correctness 缺陷，也不能推出 5% 以上成本。没有收益证据时，不把新增 C 补丁作为收尾条件。
4. **立即 spill 的近零 memory_build 已有源码与原日志解释。**并行 4 MiB 在共享区扣除 3 MiB 后，再遇到 1 MiB 预留，0 tuple spill 符合现有逻辑；新 8 MiB 中途 spill 诊断已覆盖更代表性的路径。无需把它误列为时钟 bug，也不能事后把旧正式场景换成 8 MiB。
5. **旧 CPU 校准的时长局限真实存在。**50M 上限使探测只有 0.192–0.311 秒，不能证明数秒 HNSW 执行状态。新 construction profile 以固定 1.5B、1 预热+12 正式、300 秒预算补充这一局限；`run.py:22–38, 165–215, 270–293` 和事前方法相符。它尚须完成本轮运行/独立审计后才能记录为已完成。即使稳定也不证明 HNSW 环境独立、受控或成本低于 5%；不稳定亦不证明硬件频率或 C 根因。

## 仍需实际完成的工作与停止边界

**本轮已授权的具体工作仍要完成：**新 construction 工具改动的对应离线校验、固定整批执行、原始值/身份/时钟/checksum/编译输出/源码归档与清理独立核验，并更新当前状态文档。若失败，原样保存失败、核对资源，不补样拼接为成功。这里列的是本轮尚待核实的动作，不预判父任务正在进行的结果。

**当前没有依据再新增必交产品功能或 C 优化。**若本轮未识别到可以区分原因的新干预或确定性缺陷，技术开发可以收口为“已验证功能候选及可复现证据，整体性能验收未关闭”。这条边界不等于用户原本期待的全部验收已达成，也不自动授权提交、联系负责人或变更 5% 参考线。

若继续追求三项 5% 证明，新工作应先明确要检验的机制、干预、固定预算与独立统计单位，再运行完整独立验证；只增加样本、改变实例/时段直到通过不是新的开发成果。既有 75 次机制/桥接与 CPU 探测只作探索性证据，不并入正式样本，不归一化旧结果，不把同镜像跨批约 12 秒→6 秒宣称为 C 优化收益。

**交付准备可形成具体包：**候选补丁、工具及复现入口、版本和输入身份、成功与失败验证记录、三项未证明及模型/场景限制。外部提交/负责人确认按用户要求留后；只有最终目录/分支或实现发生变化时，才产生相应集成适配和重编译回归。未知接收形态不应通过臆造目录或提前联系来“完成”。

README 第 16–31 行、收尾第 198–215 行、整体 Review 第 346–380 行目前已有正确边界。本轮完成后需追加新校准结果与其限制，不能覆盖旧短探测或改写为“全部开发验收完成”。本审查没有发现除此之外有证据支持的本地必做功能遗漏。

## 本次读取身份

- `pgvector/src/hnsw.c`：`71da2aafc9ac944d7667b878b9509a944b810b1d44968d4cdedf7f04dd4c2c87`
- `pgvector/src/hnsw.h`：`cac0e2d2e20a1006cfa4b131144297ac97f2bd5b174d13aa7477edb6652257d8`
- `pgvector/src/hnswbuild.c`：`f7564f02a18c481ad5050e9e0d7e09597652fbce7ce30e3c88d582c753e470ee`
- `dev/cpu_calibration/run.py`：`628097fa5c663810aac7916c95ce03aef908a2abb7be17fefd5ee1266cb62636`
- `dev/cpu_calibration/calibrate.c`：`e69d3fa14355179a359795440bd8ef3e1aa533ec7639aaf2c71e14d171567a6e`
- `experiments/hnsw_build/overhead_crossover.py`：`38f1cac8294d13140f4cda8f83a46d5f301bebac6b231e752b9dbdaf2037c3d9`
- `docs/2026-09-13-construction-duration-calibration.md`：`7741df420c394ef811db25f4c67cde752d34ce8c52024998af572d11a947a74f`
