# 仓库整理后的最小复现验证

日期：2026-09-14。对应[使用说明](../../../docs/usage.md)。

从拟交付文件导出独立目录，未复制作者已有的 `OpenTenBase/`、`pgvector/`、数据库
或虚拟环境；通过 `dev/prepare_sources.py` 从上游下载固定 commit，应用冻结补丁
与新增 TAP 文件，并核对候选摘要。源码准备与 `--check` 均通过，重复准备会拒绝
覆盖已有源码。

Docker 使用新目录作为构建上下文，正常复用现有层缓存，没有强制无缓存重编译。
生成的运行镜像和测试镜像 ID 均与 9/6 冻结记录一致。构建日志在本目录保留。
本次验证的是交付输入完整性和最小运行流程，不是新的性能基准或全环境兼容性验收。

## 验证结果

| 项目 | 结果 |
|---|---|
| 上游 commit、冻结 patch、新增 TAP 和五个候选文件摘要 | 全部一致 |
| 新增计时 TAP | 1 文件、221 断言通过 |
| Python 工具测试 | 120 项通过，无跳过；Python 3.14.7，使用已有的冻结依赖环境 |
| 低内存构建 | 10,000 × 32、1MB；落盘，内部计时完整，HNSW 查询计划验证通过 |
| 提高内存后复验 | 同负载、64MB；未落盘，内部计时完整，HNSW 查询计划验证通过 |
| Recall 示例 | 同负载、20 个独立查询、ef_search 40/100/200；报告与结果生成成功 |
| 清理 | 三个示例各自的临时容器、网络与数据卷清理完成；TAP 容器自动移除 |

三个示例各运行一次，仅验证功能与文档命令；不据其耗时比例作稳定性能结论。
原 9/6 产品 C/TAP、既有实验结果和汇报材料未改动，正式开销的 9 项支持与
3 项未证明结论保持不变。

## 原始输出

- [结构化验证记录](validation.json)、[重建源码身份](source-provenance.json)。
- [计时专项测试](tap-049.log)、[Python 测试](python-tests.log)。
- [测试镜像构建日志](docker-build-test.log)、[运行镜像构建日志](docker-build-runtime.log)。
- [低内存诊断](runs/20260914T034901Z-review-low/diagnostic.md)。
- [高内存复验](runs/20260914T035007Z-review-high/diagnostic.md)。
- [Recall 示例](runs/20260914T035154Z-review-recall/diagnostic.md)。

各运行目录保留完整 `summary.json`、`progress.csv`、构建日志和可选 `recall.csv`。
这份验证记录只说明本地候选已验证，不代表已推送远端、已提交活动或已获评审接收。
