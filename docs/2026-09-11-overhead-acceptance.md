# 整套诊断的额外开销验收（9/12 复核：续验仍未通过）

后续进展：用户9/12授权完成开发与本地验证，已完成[方法审查并固定新的正逆序协议](2026-09-12-performance-method-review.md)。
新正式首批因供电变化中止；恢复AC后的完整重跑于9/13 06:45结束，独立审计通过，
9/12项支持低于5%、整体仍未验收。结果见[本轮开发收尾记录](2026-09-12-development-closeout.md)。
下文保留旧协议及其未通过结果；旧A/A和原入口保护没有被改成通过。

2026-09-11 用户明确要求完成剩余开销问题，再一起 Review。本轮是新的、有上限的
验收，不覆盖先前失败或未建立支持的结果。没有修改业务库、提交代码、推送个人仓库或上游。

## 要回答的问题

相对原版 pgvector 0.8.6，整套构建诊断在默认关闭计时、开启计时、开启并每秒只读观察
三种使用方式下，是否足够轻量？之前的相邻诊断版本比较不能单独回答这个问题。
本轮自设参考线仍为 5%，不冒充导师确认过的硬门槛。

## 最新结论（9/12 复核，测量于9/11 19:43结束）

**用户确认窗口后的完整复验仍未通过，诊断总开销低于5%尚未得到证明。**
本次不是中断：64次测量+64次预热全部完成，16个测试实例及临时卷已清理。
离线重读原始记录、日志、身份、源码归档与统计，均与汇总一致。没有启动正式
原版/诊断对照，没有修改C或门槛，也不自动再开一轮A/A。

| 场景 | 同版本配对中位变化 | 中位比值下界变化 | 上界变化 | 最大绝对变化（n=8时即样本p90） | 筛查 |
|---|---:|---:|---:|---:|---|
| 串行无spill | +0.40% | −1.09% | +4.23% | 14.05% | 未通过 |
| 串行spill | −1.60% | −9.98% | +10.83% | 16.96% | 未通过 |
| 并行无spill | −3.17% | −28.90% | +6.47% | 35.80% | 未通过 |
| 并行spill | +2.39% | −2.46% | +21.93% | 54.23% | 未通过 |

这些是同一候选关闭计时的 `aa_b/aa_a`，不是诊断成本。每项上下界各为单侧95%
筛查界限，不是联合置信区间。四个中位点估计虽都在±5%以内，仍不等于开销通过。
全部128次测量前后记录AC，两种时钟最大差0.01450秒；边界采样的宿主一分钟负载
范围2.26–6.89，没有非零cgroup限流、memory max、OOM或OOM kill计数。
这不足以证明绝对空闲，但已说明上次45.83的负载高峰不是观察到波动的必要条件；
不能继续把接电或腾出窗口当作唯一缺项，也不能据此直接锁定频率、调度或建图为根因。

最大差异在第3面板并行spill：5.714s与8.812s，CPU时间17.051s与26.370s，
索引大小相差一个8KiB页。并行拓扑未被测试seed完全固定是已知限制，但大小差异
不能证明它解释了全部时间差。串行无spill也有14.05%的单次配对差异。

源码、功能与工具的已完成结论保留：98项工具测试、常规版16次构建及20项功能检查；
9/6的C/TAP候选不变。新留痕为
`dev/snapshots/20260911-overhead-acceptance/checkpoint-after-approved-window.json`，
其中三个完整A/A分别3/4、0/4、0/4场景通过，`formal=null`，状态仍为
`performance_pending`。两个旧检查点不覆盖。

## 续验启动记录（9/11 19:18，保留历史）

用户在获知上一轮未通过及需要连续低负载窗口后明确要求“继续吧”。开始前逐一核对
上一收尾检查点的1,081个文件，全部哈希相同；当前接电，宿主一分钟负载为3.45，
但单次读数不能证明整个窗口稳定。环境快照为
`dev/snapshots/20260911-overhead-acceptance/environment-20260911T1119Z-approved-window.json`。
新运行 `20260911T111859Z-bounded-acceptance-aa` 沿用最后配置：scale1、客体CPU0–9，
相同镜像、五个测量脚本、8面板和全部门槛，单独完成64测量+64预热。
这是用户确认新窗口后的复验，不更换配置、不与旧数据拼接，也不自动反复抽样到通过。
只有完整 A/A 通过才执行既定24面板正式对照；旧失败和旧检查点原样保留。

## 上一轮结论（9/11 19:16 留痕）

**本轮已结束，但“诊断总开销低于 5%”仍未得到证明，不能算完成该项要求。**
验收工具与常规功能检查完成；新建对照环境已统一编译，
98 项工具测试通过，常规编译版 16 次构建及配套共 20 项检查全部通过；独立离线复核
也核对了原始日志、查询计划、4 次真实只读观察、归档源码和实例清理。
完整的四客体 CPU A/A 有 3/4 场景通过；两倍规模提前记失败；最后的全客体 CPU A/A
于 19:06 完整结束，0/4 场景通过。没有拼接中断样本，没有启动正式原版/诊断对照。
所有测试实例及临时卷已清理，原始证据保留。

本轮没有新增 C 改动，也没有发现可归因后直接修复的 C 性能缺陷。最后两轮中断之后，
新的完整结果说明：问题不只是合盖或断电，接电仍可能有很大波动。A/A 比的是同一版本，
因此它既不能证明诊断代码很慢，也不能证明诊断足够轻量。按本轮承诺停止继续换配置加测。

| 你要 Review 的问题 | 当前答案 |
|---|---|
| 是否已建立原版/候选的公平构建与测量入口？ | 已完成；同一工具链、运行时、数据和资源协议，原版不是旧诊断版本 |
| 功能是否仍可用？ | 常规编译版 16 次构建及共 20 项功能检查通过；不把功能耗时当性能结论 |
| 默认关闭、开启计时、开启并观察是否均已证明 <5%？ | **没有**；前置 A/A 未通过，正式 12 项对照尚未执行 |
| 现在是否应该继续盲目改 C？ | 不应该；尚无能够归因到 C 的缺陷，不用代码变动掩盖测量不确定性 |

当时先看下方“最后一项校准的完整结果”，再看协议。当时最新留痕为
`dev/snapshots/20260911-overhead-acceptance/checkpoint-after-final-screen.json`，
状态仍是 `performance_pending`，不是性能验收通过；旧检查点不覆盖。

## 开始测量前锁定的协议

- 同一 OpenTenBase 18.6、编译器、编译选项、依赖、运行时基础镜像，分别从原版提交和
  当前候选重建扩展。编译过程不更新系统包。原版/候选各有 seed42 受控版与常规版。
- 当前 C/TAP 不变；源文件、编译输入、基础层与最终镜像身份见
  `dev/snapshots/20260911-overhead-acceptance/build-manifest.json`。
- 四场景：串行/2 worker × spill/无 spill。数据为固定 SQL seed 的 32 维合成向量；
  spill 3 万行，串行 1MB、并行 4MB；无 spill 10 万行、256MB。m16、ef_construction64。
  每个场景都核验实际 worker 数、spill 提示及开关行为。
- 每个配对面板使用新建的本地实例；各条件相同数据摘要、相同次数预热与测量；每次建索引
  用新连接，先删除上次索引并完成 CHECKPOINT。同一时刻只运行一次建索引。
- 每实例 2GiB 内存、无额外 swap，起始 Linux 客体 CPU 亲和性 0–3，最后校准为 0–9。
  各次配置分别记入 protocol，绝不混合统计。不是 Mac 物理核心独占。
  保存测量前后 cgroup CPU/内存/I/O 计数及宿主 load average；不事后删除异常值。
- 运行期间要求接电，逐次检查前后供电状态；任务作用域内用 `caffeinate -i` 防止空闲
  休眠，不改变系统设置，也不能阻止用户合盖休眠。同时记录实时时钟与单调计时，
  两者差值超过 1 秒即拒绝该轮。任何中断都保留原始失败，不把剩余样本拼成通过结果。
- A/A：相同候选关闭计时的两个实例，固定 8 面板、每场景 8 配对，64 正式 + 64 预热。
  每场景单侧 95% 中位比值上界 <1.05、下界 >0.95，且样本绝对变化 p90 ≤10% 才继续。
  A/A 是环境筛查，不是正式 12 项联合性能保证。
- 正式：固定 24 面板、384 正式 +384 预热。每场景四条件的全部 24 排列各一次，并随机化
  时间顺序；场景顺序也平衡。独立统计单位是面板，不把预热或重复观测当独立样本。
- 12 项“候选条件/原版”的配对比值总体中位数，采用分布无关的单侧顺序统计量上界，
  每项置信度 1−0.05/12，用 Bonferroni 控制家族误差率 ≤5%（以面板独立为前提）。
  上界必须 <1.05；观察组每个面板还必须真正采到构建进度。
- 不按中途效果追加样本、不改 5% 线、不删慢样本。A/A 不通过先定位环境，不启动正式。
  仅允许在正式开始前用 A/A 校准数据规模（1 或 2 倍）及预先支持的客体 CPU 范围，
  每次单独留档；不能用正式效果选择环境。若无法获得可信精度，不能宣称通过。

## 解释边界

本实验使用 seed42 受控编译，固定串行 HNSW 随机数；并行图仍可能变化。
常规构建另外做功能检查。这里不是生产 SLA、所有机器/数据/参数的最坏开销保证，
也不替代已经完成的官方 GloVe 业务数据验证。正式结果完成前状态保持“未验收”。

## 自检留痕

首次 Docker 构建因把本地 image ID 当作 FROM 地址失败；改为构建前后核对固定本地标签
身份并核对公共基础层后，第二次四镜像构建通过。没有 C 编译缺陷。
首次流程 smoke 在 TCP 连接阶段失败：新容器访问规则缺少 Docker bridge gateway。
修复为仅允许该 gateway /32、postgres 数据库、两个测试账号的 SCRAM 密码访问。
端口只绑定 127.0.0.1，临时密码不写进结果。失败目录保留，未产生性能样本。

## 9/11 中途核查

- 四场景 smoke `20260911T014752Z-bounded-acceptance-smoke`：8 正式 +8 预热均完成，
  两个实例清理；离线核对日志、源码归档、身份及原始结果通过。
- 一次过早启动的 A/A 被端口占用保护拒绝，未产生测量、未动现有实例：
  `20260911T015041Z-bounded-acceptance-aa`。
- A/A `20260911T015059Z-bounded-acceptance-aa` 在第 3 面板中断；已存 19 正式、
  19 预热及另一次失败构建，全部 6 个实例清理。**这不是通过的 A/A，也不作版本开销推断。**
- 当时 `pmset -g log` 显示 09:59:53 因 `Clamshell Sleep` 进入休眠，持续 949 秒，
  10:15:42 开盖恢复；供电转为电池。失败 SQL 为 `QueryCanceled:57014`，与配置的
  180 秒语句超时相符。客户端单调计时却只有 9.891 秒，说明仅记这种计时会漏掉宿主
  休眠，不能用它断言数据库异常变快/变慢。没有保存该已清理实例的服务器取消详情，
  因而不声称已从服务器日志独立确认取消原因。
- 新补前述供电/双时钟保护，且完整保留旧入口归档和失败结果。98 项工具单元测试通过。
- 常规编译版的首次功能检查遇到预期的 schema 权限拒绝，测试原本用
  `has_table_privilege` 名称查找，误把 schema 拒绝当异常；改为直接确认读取被 42501 拒绝。
  原失败目录 `20260911T052925Z-bounded-acceptance-normal` 保留，4 个实例已清理。
  后续 `20260911T053006Z-bounded-acceptance-normal` 已完成 16 次构建、20 项检查，
  4 个实例清理。观察条件四场景均真正采到进度；每次查询都核对有效 HNSW 索引与计划。
  不拿电池环境中的这些耗时作开销结论。
- 常规功能检查期间曾对验证函数做等价提取，已按运行前记录的 SHA-256 精确恢复三个
  运行源文件并归档，明确标为运行后恢复，不冒充运行前归档。后续入口改为启动时归档；
  正式性能入口原本已启动时归档，新的 A/A/正式运行中不得修改测量代码。

当时正式新旧对照尚未启动，性能验收仍未关闭。计划在接电、开盖的连续运行窗口，
通过新的 A/A 筛查后才执行已固定的 24 面板协议；不会因用户暂时离开而删除失败。
当时的留痕审计是 `dev/snapshots/20260911-overhead-acceptance/checkpoint.json`，
状态特意使用 `performance_pending`，不是正式验收 `passed`。完成正式结果后再生成
另一个新审计，不覆盖这个检查点。

## 恢复接电后的完整 A/A（14:12 完成）

`20260911T055049Z-bounded-acceptance-aa`：完整 64 正式 +64 预热，16 个实例均已清理，
原始日志、源码归档、身份、两种时钟与供电检查、统计汇总已离线复核。

| 场景 | 配对中位变化 | 中位比值下界变化 | 上界变化 | 最大绝对配对变化（n=8 时即样本 p90） | 门槛 |
|---|---:|---:|---:|---:|---|
| 串行无 spill | −0.06% | −0.86% | +0.86% | 4.92% | 通过 |
| 串行 spill | +0.93% | −1.32% | +4.16% | 8.11% | 通过 |
| 并行无 spill | +0.50% | −2.34% | +4.14% | 9.37% | 通过 |
| 并行 spill | +0.99% | −3.07% | +3.50% | 14.64% | **未通过** |

上下界各自是单侧 95% 筛查；不是正式 12 项联合保证，也不是这里已量得诊断成本。
失败来自并行 spill 最后一组 5.452s /6.387s 的同版本配对；CPU 时间分别16.261s /19.032s，
索引相差一个 8KiB 页。没有时钟跳变或供电变化；不能直接归因等待或诊断 C 实现。
不删除这一组、不放宽样本 p90≤10% 门槛，不以其余三场景通过代替整体通过。

14:13 启动 `20260911T061330Z-bounded-acceptance-aa`，保持相同源码、镜像、CPU 与统计
规则，仅按预先允许的 `--scale 2` 把数据改为20万/6万行，以检验更长任务是否降低短任务
及并行建图的波动。这不是对正式新旧效果的事后选参。启动时计划若它通过，后续正式也
必须采用两倍规模；实际结果是下节记录的提前失败，不再等待这条线或把它作为 gate。

### 两倍规模提前失败与最后一项校准

两倍规模首组并行 spill 已有 24.75% 的绝对配对差异。固定 n=8 的样本 p90 是最大值，
因此后续测量不可能挽回 ≤10% 门槛。14:28 明确偏离原“全部跑完”的计划，提前记失败
并停止；保留 18 正式、19 预热及 1 次中断原始记录，6 个实例清理。
详见该目录 `deviation.md`；原 protocol 不改写，不用残缺结果构造完整统计或通过 gate。

14:29 启动 `20260911T062955Z-bounded-acceptance-aa`：恢复原 10万/3万行数据，仅把
客体 CPU 亲和性改成预先支持的 `0-9`，检验是否能避免限制在四个客体 CPU 上造成的
测量影响。不是 Mac 物理核心独占，也不是已经确认 CPU 亲和性是根因。
这是本轮最后一项环境校准；5% 和 p90≤10% 门槛不变，不再遍历其他组合。
本轮完整通过后，正式必须使用相同 `--affinity 0-9`；未通过则不启动正式。

14:35 该执行检测到 AC→Battery 后主动停测，保留17正式+17有效预热，以及一次
受供电切换影响的原始预热，6实例清理。该次 SQL 实际成功，失败来自供电保护；
之后才发生合盖休眠。详见原目录 `interruption.md`。
18:29 用户要求继续，确认已接电且五个测量脚本与原 protocol 哈希相同后，启动
`20260911T102954Z-bounded-acceptance-aa`。这是同一最后校准配置的完整重跑，不是
增加一种新配置，也不拼接此前部分样本。完整结果如下。

## 最后一项校准的完整结果（19:06 结束）

`20260911T102954Z-bounded-acceptance-aa`：原规模、客体 CPU 0–9，8 个新面板，
64 次正式测量 +64 次预热全部完成，16 个实例清理。离线从原始日志复算统计，
核对镜像、数据摘要、源码归档、实际 worker 数、spill 及清理记录，均与汇总一致。

| 场景 | 配对中位变化 | 中位比值下界变化 | 上界变化 | 最大绝对配对变化（n=8 时即样本 p90） | 门槛 |
|---|---:|---:|---:|---:|---|
| 串行无 spill | −4.28% | −13.39% | +9.00% | 40.01% | 未通过 |
| 串行 spill | +2.78% | −4.36% | +37.81% | 66.71% | 未通过 |
| 并行无 spill | +5.47% | −3.80% | +16.90% | 18.28% | 未通过 |
| 并行 spill | +8.07% | −7.79% | +30.80% | 44.98% | 未通过 |

表中是 `aa_b / aa_a`，**不是“有诊断 / 无诊断”**。上下界各自为单侧 95% 筛查，
不是联合置信区间。所有原始配对都保留，不能把中位变化恰好落在 ±5% 的场景当成通过。

128 次测量前后均记录为 AC；两种时钟最大差值 0.02448 秒，未越过 1 秒保护阈值。
边界计数没有非零的 cgroup 限流、memory max、OOM 或 OOM kill；边界内存最大
282.42MiB（不是连续观测到的峰值）。宿主一分钟 load average 的边界采样最大值为
45.83，出现在第 7 面板约18:59。它说明不能把这次窗口当作已验证空闲，但不等于
CPU 利用率，也不能仅凭这个值确定哪个进程、物理核心、频率或存储是根因。

一个可直接复查的反例是第 6 面板串行 spill：同镜像、同数据、同索引大小，
`aa_a` 为24.704s、`aa_b` 为41.184s；cgroup CPU 时间分别24.720s、41.194s。
串行无 spill 同面板也从55.758s变为33.450s。串行同版本已有大幅变化，不能把
整个结果仅归因于并行 HNSW 建图随机性；CPU 时间同步变化也不足以直接证明频率根因。
这些是解释为什么没有进入正式验收的证据，不是挑选快样本或修改统计规则的理由。

收尾只为离线审计补充了可重复的 `--aa` 入口，让失败的完整筛查也写入新检查点；
它不会把失败改成通过。五个测量/统计脚本、C 候选和本次运行协议未改变。

## 剩余项怎样关闭（9/12 更新）

本次窗口复验也已完成但未通过，不再以“请再腾一个窗口”代替方法判断。
下一步应先小范围Review测量方案，而不是重复本协议或盲目改C：

1. 区分业务要验证的“相对原版的总体中位成本上界<5%”与当前额外的A/A准入要求。
   n=8时样本p90实际是最大值，因此准入还要求所有8个配对绝对差≤10%。两者不是
   同一命题；A/A失败不证明正式对照必然无法测量。该准入可能过于苛刻，需要Review，
   但不能在看过结果后静默删掉它来宣称通过。本次除串行无spill外，其余场景也未过
   中位上下界要求，所以仅删除p90条件同样不能让本次完整通过。
2. 核对并行建图变化、独立面板和顺序平衡能否支持希望作出的因果解释；如果要调整
   设计，应先写清新的固定预算、主验收标准和允许作出的结论，再另行确认执行。
3. 原始结果继续按旧协议记未通过；C、5%主参考线、当前脚本和正式入口保护保持原样。
   在方案Review前没有新的测量计划，也不把当前电池状态当作昨天失败的原因。

下面保留的是上一轮提出并已执行过的窗口建议，**不是再次请求重复执行**。

### 9/11 当时的窗口建议（历史）

本轮预算已用完，不自动重跑、不继续遍历配置、不放宽 5% 或 p90 门槛。
用户现在可以 Review 功能、实现、实验方法和失败证据，但不应勾选“开销已验收”。
若继续关闭这一项，先由用户确认能否安排**无其他重负载的连续本地运行窗口**，
并审阅当前验收协议；不需要先取得组内服务器，也不会擅自停止其他程序或改系统设置。
按这次耗时外推，A/A 加正式约需4–5小时，实际随负载变化；接电开盖只是必要控制，
不是保证通过。只有环境条件确有改变后才值得再测，不能为了碰到通过重复抽样。

下面保留的是经确认后复现最后配置的入口，**不是本轮继续执行计划**：

```bash
.venv-glove/bin/python experiments/hnsw_build/acceptance.py aa --affinity 0-9
```

新的 A/A 必须完整且 `analysis.all_supported=true` 才能作为正式实验 gate；
目前没有任何可用 gate。正式仍是相同配置的24面板、12项联合上界判定；失败则保留
未验收，不追加样本。若通过，再用 `audit_acceptance.py --formal` 生成新的验收文件。
原方案中的“发现可归因的 C 回归再修复”同样保留，不能仅凭噪声制造修复任务。
